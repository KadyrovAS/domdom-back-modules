method:
  metrics:
    enabled: true
    prefix: "app"
    histogram: true
    percentiles: [0.5, 0.95, 0.99]

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics,prometheus
  metrics:
    tags:
      application: method-metrics-demo
      environment: ${ENVIRONMENT:local}